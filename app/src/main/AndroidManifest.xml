<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.setting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Setting">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".Notifications"
            android:label="@string/Notifications"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MoreAlerts"
            android:label="@string/Alerts"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".Account"
            android:label="@string/Account"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".Password"
            android:label="@string/Password"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MARATHIMATRIMONY"
            android:label="@string/Delete_Account"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".Logout"
            android:label="@string/logout"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".PrivacySetting"
            android:label="@string/PRIVACYSETTING"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".Mobile"
            android:label="@string/PRIVACYSETTING"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".Photo"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".Horoscope"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".VideoChat"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".AdvancedSetting"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".CallPreferences"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".BlockedProfiles"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".IgnoredProfiles"
            android:label="@string/PRIVACY"
            android:parentActivityName=".MainActivity" />




























    </application>

</manifest>